import { HeadingsDef } from "./types/headings";

const headings: HeadingsDef = {
    fontWeight: 400,
    sizes: {
        h1: { fontWeight: 200, fontSize: 32, lineHeight: 1.5 }
    }
};

export default headings;
